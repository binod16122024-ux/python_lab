encoded = [18, 24, 21, 19, 47, 44, 59, 38, 11, 57, 53, 39, 32, 49, 38, 41]
key = 84

# TODO: each byte in 'encoded' was produced by XOR-ing a character with 'key'.
# XOR every byte with 'key' again, turn the results back into characters,
# and join them to reveal the flag.
