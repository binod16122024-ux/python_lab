text = "nfnpywfgfufuwjynnnwfppujygfnwfwuwguugfgnnfup"
counts = {}

# TODO: count how many times each character in 'text' appears, storing the
# tally in 'counts'.

top_char = None
# TODO: find the character in 'counts' with the highest count and store it
# in 'top_char'.

print(f"FLAG{{freq_{top_char}{counts[top_char]}}}")
